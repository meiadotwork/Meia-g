{
  "name": "meia-g",
  "version": "1.0.0",
  "private": true,
  "description": "meia-g.art — website for the artist Meia G",
  "scripts": {
    "build": "node scripts/build.js",
    "build:pages": "node scripts/build.js --pages",
    "check": "node scripts/check.js",
    "serve": "node scripts/serve.js"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
